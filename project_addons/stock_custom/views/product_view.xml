<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- product.template inherit form view -->
    <record id="product_template_form_view_abc" model="ir.ui.view">
        <field name="name">product_template_form_view_abc</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <field name="categ_id" position="after">
                <field name="abc_classification"/>
            </field>
        </field>
    </record>



    <!-- product.product inherit form view -->
    <!-- <record id="view_id" model="ir.ui.view">
        <field name="name">product.template.aecoc.view.form</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view"/>
        <field name="arch" type="xml">
            <field name="categ_id" position="after">
                <field name="abc_classification"/>
            </field>
        </field>
    </record> -->

    <!-- product.template inherit search view -->
    <record id="product_template_search_view_aecoc" model="ir.ui.view">
        <field name="name">product.template.inherit.view.search</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <field name="categ_id" position="after">
                <field name="abc_classification"/>
            </field>

            <filter name="activities_upcoming_all" position="after">
                 <filter string="ABC Classification" name="groupby_abc" domain="[]" context="{'group_by': 'abc_classification'}"/>
            </filter>
        </field>
    </record>
</odoo>