<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_packaging_pick_from_package_wzd" model="ir.ui.view">
        <field name="name">view.packaging.pick.from.package.wzd</field>
        <field name="model">packaging.pick.from.package.wzd</field>
        <field name="arch" type="xml">
            <form string="Asistente para mover paquetes">
                <header>

                </header>
                <separator string="Paquete"/>
                <h2>
                    <field name="package_id"/>
                </h2>
                <label string="Indica el albarán de cliente asociado"/>

                <group string="">
                    <field name="batch_picking_id"/>
                </group>

                <footer>
                    <button name="create_pick" string="Mover" type="object" class="oe_highlight"/>
                    <button string="Cancel" class="oe_link" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <act_window name="Asistente de albaran de palets"
                res_model="packaging.pick.from.package.wzd"
                src_model="stock.quant.package"
                view_mode="form"
                target="new"
                key2="client_action_multi"
                id="act_packaging_pick_from_package_wzd"/>

</odoo>